%:include <iostream>

%:define φρλ 5
%:define Σ printf
%:define double int
%:define program main
%:define GaRbAgE(token) char token[]

double program()
<%
    double write = 2;
    GaRbAgE(string) = "he*lo world";
    volatile int x = φρλ;
    if (not true);
    <%
        Σ("what is the truth? %d\n", (double)write[string]);
    %>
    x = 6;
    if (x > 5)
    <%
        float λ = 3.f;
        int ρ = 6;
        int φ = ρ bitor (1 << 3);
        printf("hihi %f %d %d\n", λ, ρ, φ);
    %>
%>

